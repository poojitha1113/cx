git init
git config user.name
git config user.email
git config user.name "poojitha1113"
git config user.email "poojtiha.gutha1113@gmail.com"
cd d:
git clone " "
cd cx
git add --all
git commit -m "test"
git push
[git remote set-url origin https://poojitha1113@github.com/poojitha1113/cx.git] any permission issues

git check out:
new branch create new branch new repo
and clone the project
cd to folodr
and git branch -a
git checkout test
work on it 
